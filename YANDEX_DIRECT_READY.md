# ✅ Яндекс.Директ модуль настроен и готов к работе

## Что настроено:

1. ✅ **Переменные окружения:**
   - `YANDEX_DIRECT_ACCESS_TOKEN` - токен API
   - `YANDEX_DIRECT_LOGIN` - логин рекламодателя
   - `AUTO_OPTIMIZE_SECRET` - секретный ключ для защиты

2. ✅ **Сервис перезапущен** - новые переменные загружены

3. ✅ **API endpoints работают:**
   - `GET /api/yandex-direct/campaigns` - список кампаний
   - `POST /api/yandex-direct/campaigns/optimize` - оптимизация
   - `POST /api/yandex-direct/auto-optimize` - автоматическая оптимизация

## Использование:

### Ручная оптимизация:
```bash
curl -X POST https://grifun.ru/api/yandex-direct/campaigns/optimize \
  -H "Content-Type: application/json" \
  -d '{"days": 7}'
```

### Автоматическая оптимизация (защищена ключом):
```bash
curl -X POST https://grifun.ru/api/yandex-direct/auto-optimize?days=7 \
  -H "Authorization: Bearer 1a7b507793112040355a6e9a84863e8d0a4031b2ec747eca28f77acb15d3feb4"
```

## Настройка автоматической оптимизации (опционально):

Если хотите, чтобы оптимизация запускалась автоматически каждый день:

```bash
# Запустите скрипт настройки cron
bash /home/mikhail/vibecoding_www/scripts/setup-yandex-direct-cron.sh
```

Или вручную добавьте в crontab:
```bash
crontab -e
# Добавьте строку:
0 9 * * * curl -X POST https://grifun.ru/api/yandex-direct/auto-optimize?days=7 -H 'Authorization: Bearer 1a7b507793112040355a6e9a84863e8d0a4031b2ec747eca28f77acb15d3feb4' > /dev/null 2>&1
```

## Мониторинг:

Просмотр логов оптимизации:
```bash
sudo journalctl -u vibecoding -f | grep "Yandex Direct"
```

## Готово! 🎉

Модуль полностью настроен и готов к работе. Система будет анализировать ваши кампании и предлагать оптимизации.






