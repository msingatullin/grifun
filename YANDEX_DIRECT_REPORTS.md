# 📊 Система отчетов по оптимизации Яндекс.Директа

## Как работает оптимизация

### Процесс оптимизации:

1. **Сбор данных:**
   - Получение списка активных кампаний из Яндекс.Директа
   - Сбор статистики за указанный период (по умолчанию 7 дней)
   - Анализ метрик: показы, клики, CTR, стоимость клика, конверсии

2. **AI-анализ:**
   - Передача данных в OpenAI GPT-4o-mini
   - Анализ эффективности каждой кампании
   - Генерация рекомендаций и предложений по улучшению

3. **Создание отчета:**
   - Сохранение результатов в файл (`data/optimizations/`)
   - Формирование структурированного отчета с:
     * Оценкой эффективности (score 0-100)
     * Кратким анализом проблем и возможностей
     * Конкретными рекомендациями
     * Предложенными изменениями с приоритетами

4. **Логирование:**
   - Все результаты сохраняются в логи сервиса
   - Отчеты доступны через API и веб-интерфейс

## Как часто происходит оптимизация

### Текущие настройки:

**Автоматическая оптимизация:** Не настроена (cron задача отсутствует)

**Ручная оптимизация:** По запросу через API или веб-интерфейс

### Рекомендуемое расписание:

- **Ежедневно в 9:00 (MSK)** - для регулярного мониторинга
- **Еженедельно (понедельник 9:00)** - для глубокого анализа
- **После изменений в кампаниях** - для проверки эффективности

### Настройка автоматической оптимизации:

```bash
# Добавьте в crontab (crontab -e)
0 9 * * * curl -X POST https://grifun.ru/api/yandex-direct/auto-optimize?days=7 \
  -H 'Authorization: Bearer 1a7b507793112040355a6e9a84863e8d0a4031b2ec747eca28f77acb15d3feb4' \
  > /dev/null 2>&1
```

Или используйте скрипт:
```bash
bash /home/mikhail/vibecoding_www/scripts/setup-yandex-direct-cron.sh
```

## Просмотр отчетов

### Веб-интерфейс:

Откройте в браузере:
```
https://grifun.ru/yandex-direct
```

**Что показывает интерфейс:**
- 📊 Сводка по всем кампаниям (средний score, количество рекомендаций)
- 📅 Информация о периоде анализа
- 🎯 Детальный анализ каждой кампании:
  * Оценка эффективности (score)
  * AI-анализ проблем и возможностей
  * Конкретные рекомендации
  * Предложенные изменения с обоснованием и приоритетом

### API endpoints:

**Получить последний отчет:**
```bash
curl https://grifun.ru/api/yandex-direct/reports?latest=true
```

**Получить все отчеты:**
```bash
curl https://grifun.ru/api/yandex-direct/reports
```

**Получить конкретный отчет:**
```bash
curl https://grifun.ru/api/yandex-direct/reports?id=opt_1234567890
```

**Получить отчет в формате Markdown:**
```bash
curl https://grifun.ru/api/yandex-direct/reports?latest=true&format=markdown
```

## Структура отчета

### Что было (до оптимизации):
- Статистика кампаний за период
- Текущие настройки (бюджет, ставки, ключевые слова)
- Проблемные метрики (низкий CTR, высокая стоимость клика)

### Что сделал AI:
- Проанализировал эффективность каждой кампании
- Выявил проблемы и возможности для улучшения
- Сгенерировал конкретные рекомендации

### Почему это было сделано:
- Каждое предложенное изменение содержит обоснование
- Указан приоритет (high/medium/low)
- Объяснена причина необходимости изменения

### Что это даст:
- **Увеличение CTR** - за счет оптимизации текстов и ключевых слов
- **Снижение стоимости лида** - за счет улучшения таргетинга
- **Повышение конверсии** - за счет релевантности объявлений
- **Оптимизация бюджета** - за счет правильного распределения средств

## Пример отчета

```
📊 Отчет об оптимизации кампаний Яндекс.Директа

Дата создания: 04.12.2024, 17:41:57
Период анализа: 2024-11-27 - 2024-12-04
Кампаний проанализировано: 1

📈 Сводка
- Средний score: 50.0/100
- Кампаний с низким score (<50): 0
- Кампаний с высоким score (≥70): 0
- Всего рекомендаций: 5
- Всего предложенных изменений: 1

🎯 Детальный анализ кампаний

🟡 Единая перфоманс-кампания №27 (ID: 705271290)
Оценка эффективности: 50/100

🤖 AI Анализ: Кампания находится на начальном этапе...

💡 Рекомендации:
1. Установить дневной бюджет для контроля расходов
2. Разработать релевантные текстовые объявления...
3. Определить и использовать ключевые слова...
4. Настроить таргетинг на конкретные сегменты...
5. Добавить минус-слова для исключения нерелевантных запросов

🔧 Предложенные изменения:
1. 🔴 BUDGET - Установить дневной бюджет на уровне 5000 руб.
   Почему: Это позволит контролировать расходы и оптимизировать кампанию
   Приоритет: high
```

## Где хранятся отчеты

Отчеты сохраняются в:
```
/home/mikhail/vibecoding_www/data/optimizations/
```

Формат: JSON файлы с именем `opt_<timestamp>.json`

Последний отчет всегда доступен как `latest.json`

## Мониторинг

Просмотр логов оптимизации:
```bash
sudo journalctl -u vibecoding -f | grep "Yandex Direct"
```

Проверка последнего отчета:
```bash
cat /home/mikhail/vibecoding_www/data/optimizations/latest.json | jq
```






