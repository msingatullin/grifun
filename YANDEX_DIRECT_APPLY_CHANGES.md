# 🔧 Применение изменений из отчетов

## Как применить предложенные изменения

### Вариант 1: Через веб-интерфейс (рекомендуется)

1. Откройте `https://grifun.ru/yandex-direct`
2. Найдите кампанию с предложенными изменениями
3. Для изменений типа **BUDGET** появится кнопка **"Применить"**
4. Нажмите кнопку и подтвердите действие
5. Изменение будет применено автоматически

### Вариант 2: Через API

**Применить изменение бюджета:**

```bash
curl -X POST https://grifun.ru/api/yandex-direct/apply-changes \
  -H "Content-Type: application/json" \
  -d '{
    "campaignId": 705271290,
    "changes": [
      {
        "type": "budget",
        "action": "Установить дневной бюджет на уровне 5000 руб",
        "value": 5000
      }
    ],
    "confirm": true
  }'
```

**Предварительный просмотр (без применения):**

```bash
curl -X POST https://grifun.ru/api/yandex-direct/apply-changes \
  -H "Content-Type: application/json" \
  -d '{
    "campaignId": 705271290,
    "changes": [
      {
        "type": "budget",
        "action": "Установить дневной бюджет на уровне 5000 руб",
        "value": 5000
      }
    ],
    "confirm": false
  }'
```

## Что можно применить автоматически

### ✅ Поддерживается:

- **BUDGET** - установка дневного бюджета
  - Автоматически извлекается значение из текста рекомендации
  - Применяется через API Яндекс.Директа

### ⚠️ Требует ручной настройки:

- **KEYWORDS** - добавление/изменение ключевых слов
- **NEGATIVE** - добавление минус-слов
- **AD_TEXT** - изменение текстов объявлений
- **TARGETING** - настройка таргетинга
- **BID** - изменение ставок

Эти изменения показываются в отчете, но требуют ручного применения через интерфейс Яндекс.Директа или дополнительной настройки API методов.

## Пример применения изменений из вашего отчета

Для кампании **"Единая перфоманс-кампания №27"** (ID: 705271290):

### 1. Бюджет (можно применить автоматически):

```bash
curl -X POST https://grifun.ru/api/yandex-direct/apply-changes \
  -H "Content-Type: application/json" \
  -d '{
    "campaignId": 705271290,
    "changes": [
      {
        "type": "budget",
        "action": "Установить дневной бюджет на уровне 5000 руб для начала",
        "value": 5000
      }
    ],
    "confirm": true
  }'
```

### 2. Остальные изменения (применить вручную):

**KEYWORDS:**
- Зайдите в Яндекс.Директ
- Откройте кампанию 705271290
- Добавьте ключевые слова, связанные с автоматизацией бизнеса и ИИ

**NEGATIVE:**
- Добавьте минус-слова для исключения нерелевантных запросов
- Например: "бесплатно", "скачать", "торрент" и т.д.

**AD_TEXT:**
- Создайте 3-4 варианта текстов объявлений
- Акцент на решения для B2B компаний
- Упоминание преимуществ использования ИИ

**TARGETING:**
- Настройте таргетинг на B2B компании
- Сегменты: e-commerce, сервисные компании

## Логирование примененных изменений

Все примененные изменения логируются:

```bash
sudo journalctl -u vibecoding -f | grep "apply_changes"
```

## Безопасность

- Все изменения требуют подтверждения (`confirm: true`)
- Предварительный просмотр доступен без применения
- Логирование всех операций для аудита

## Дальнейшее развитие

Планируется добавить автоматическое применение для:
- Управления ключевыми словами
- Добавления минус-слов
- Изменения текстов объявлений
- Настройки таргетинга






